# 隐私说明

最后更新：2026-07-31

Prompt Manager 不要求注册账号，不配置自有服务器，不包含广告、统计、遥测或第三方追踪代码。

## 处理的数据

- 提示词标题、正文、标签、收藏状态、时间戳和使用次数：保存在 `chrome.storage.local`。
- 标签颜色：保存在 `chrome.storage.sync`，如用户启用 Chrome 同步，可能在同一 Google/Chrome 账号的设备间同步。

## 数据传输

扩展不申请网站访问权限，不主动向开发者或第三方发送上述数据。用户主动使用“导出”功能时，浏览器会在本地生成 JSON 文件；用户主动使用“导入”功能时，文件仅在扩展页面中读取和处理。

## 数据删除

用户可以逐条删除提示词。卸载扩展或清除扩展存储通常会删除本地数据；具体行为由浏览器控制。请在卸载前导出重要内容。

## 权限

扩展仅申请 `storage` 权限，用于保存上述设置和提示词。

## 联系与变更

隐私说明的实质性变更会在仓库和版本日志中记录。问题请通过 GitHub Issues 提交，且不要附带敏感提示词。
